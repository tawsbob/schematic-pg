// Auto-generated by RouteGenerator. Do not edit manually.
import { Hono } from 'hono';
import type { AppEnv } from 'schematic-pg/api/types';
import { validateJson, validateParam, validateQuery } from 'schematic-pg/api/middleware/validate';
import { notFoundResponse } from 'schematic-pg/api/middleware/errors';
import { buildReadQuery } from 'schematic-pg/api/utils/read-query';
import { parseIncludeQuery } from 'schematic-pg/api/utils/include-query';
import { omitFields } from 'schematic-pg/api/utils/omit-fields';
import { shapeResponse, shapeResponseMany } from 'schematic-pg/api/utils/response-shape';
import { assertPolicy, mergeWhere, resolvePolicyWhere } from 'schematic-pg/api/auth/policy';
import {
  NoteCreateSchema,
  NoteUpdateSchema,
  NoteParamSchema,
  NoteListQuerySchema,
  NoteGetQuerySchema,
  NOTE_LIST_QUERY_FIELDS,
  NOTE_SORTABLE_FIELDS,
  NOTE_INCLUDABLE_RELATIONS,
  NOTE_OMIT_FIELDS,
  API_OMIT_FIELDS_BY_MODEL,
  API_RELATION_TARGETS,
} from '../schemas/validation.js';

const router = new Hono<AppEnv>();

router.get('/', validateQuery(NoteListQuerySchema), async (c) => {
  const db = c.get('db');
  const auth = c.get('auth');
  const policy = assertPolicy('Note', auth.role, 'select');
  const policyWhere = resolvePolicyWhere(policy, auth);
  const query = c.req.valid('query');
  const { where, orderBy, take, skip, include } = buildReadQuery(
    query,
    NOTE_LIST_QUERY_FIELDS,
    NOTE_SORTABLE_FIELDS,
    NOTE_INCLUDABLE_RELATIONS,
  );
  const rows = await db.note.findMany({
    where: mergeWhere(where, policyWhere),
    orderBy,
    take,
    skip,
    include,
  });
  return c.json(shapeResponseMany(rows, 'Note', API_OMIT_FIELDS_BY_MODEL, API_RELATION_TARGETS));
});

router.get('/:id', validateParam(NoteParamSchema), validateQuery(NoteGetQuerySchema), async (c) => {
  const db = c.get('db');
  const auth = c.get('auth');
  const policy = assertPolicy('Note', auth.role, 'select');
  const policyWhere = resolvePolicyWhere(policy, auth);
  const params = c.req.valid('param');
  const query = c.req.valid('query');
  const include = query.include
    ? parseIncludeQuery(query.include, NOTE_INCLUDABLE_RELATIONS)
    : undefined;
  const row = await db.note.findUnique(mergeWhere({ id: params.id }, policyWhere), { include });
  if (!row) {
    return notFoundResponse(c);
  }
  return c.json(shapeResponse(row, 'Note', API_OMIT_FIELDS_BY_MODEL, API_RELATION_TARGETS));
});

router.post('/', validateJson(NoteCreateSchema), async (c) => {
  const db = c.get('db');
  const auth = c.get('auth');
  const policy = assertPolicy('Note', auth.role, 'insert');
  const policyWhere = resolvePolicyWhere(policy, auth);
  const body = c.req.valid('json');
  const row = await db.note.create(body, { where: policyWhere });
  return c.json(omitFields(row, NOTE_OMIT_FIELDS), 201);
});

router.put('/:id', validateParam(NoteParamSchema), validateJson(NoteUpdateSchema), async (c) => {
  const db = c.get('db');
  const auth = c.get('auth');
  const policy = assertPolicy('Note', auth.role, 'update');
  const policyWhere = resolvePolicyWhere(policy, auth);
  const params = c.req.valid('param');
  const body = c.req.valid('json');
  const row = await db.note.update({ where: mergeWhere({ id: params.id }, policyWhere), data: body });
  return c.json(omitFields(row, NOTE_OMIT_FIELDS));
});

router.delete('/:id', validateParam(NoteParamSchema), async (c) => {
  const db = c.get('db');
  const auth = c.get('auth');
  const policy = assertPolicy('Note', auth.role, 'delete');
  const policyWhere = resolvePolicyWhere(policy, auth);
  const params = c.req.valid('param');
  const row = await db.note.delete(mergeWhere({ id: params.id }, policyWhere));
  return c.json(omitFields(row, NOTE_OMIT_FIELDS));
});

export default router;
