// Auto-generated by RouteGenerator. Do not edit manually.
import { Hono } from 'hono';
import type { AppEnv } from '../../src/api/types.js';
import { validateJson, validateParam } from '../../src/api/middleware/validate.js';
import { notFoundResponse } from '../../src/api/middleware/errors.js';
import {
  OrderCreateSchema,
  OrderUpdateSchema,
  OrderParamSchema,
} from '../schemas/validation.js';

const router = new Hono<AppEnv>();

router.get('/', async (c) => {
  const db = c.get('db');
  const rows = await db.order.findMany();
  return c.json(rows);
});

router.get('/:id', validateParam(OrderParamSchema), async (c) => {
  const db = c.get('db');
  const params = c.req.valid('param');
  const row = await db.order.findUnique({ id: params.id });
  if (!row) {
    return notFoundResponse(c);
  }
  return c.json(row);
});

router.post('/', validateJson(OrderCreateSchema), async (c) => {
  const db = c.get('db');
  const body = c.req.valid('json');
  const row = await db.order.create(body);
  return c.json(row, 201);
});

router.put('/:id', validateParam(OrderParamSchema), validateJson(OrderUpdateSchema), async (c) => {
  const db = c.get('db');
  const params = c.req.valid('param');
  const body = c.req.valid('json');
  const row = await db.order.update({ where: { id: params.id }, data: body });
  return c.json(row);
});

router.delete('/:id', validateParam(OrderParamSchema), async (c) => {
  const db = c.get('db');
  const params = c.req.valid('param');
  const row = await db.order.delete({ id: params.id });
  return c.json(row);
});

export default router;
