// Auto-generated by RouteGenerator. Do not edit manually.
import { Hono } from 'hono';
import type { AppEnv } from 'schematic-pg/api/types';
import { validateJson, validateParam, validateQuery } from 'schematic-pg/api/middleware/validate';
import { notFoundResponse } from 'schematic-pg/api/middleware/errors';
import { buildReadQuery } from 'schematic-pg/api/utils/read-query';
import { parseIncludeQuery } from 'schematic-pg/api/utils/include-query';
import { omitFields } from 'schematic-pg/api/utils/omit-fields';
import { shapeResponse, shapeResponseMany } from 'schematic-pg/api/utils/response-shape';
import { assertPolicy, mergeWhere, resolvePolicyWhere } from 'schematic-pg/api/auth/policy';
import {
  AnnouncementCreateSchema,
  AnnouncementUpdateSchema,
  AnnouncementParamSchema,
  AnnouncementListQuerySchema,
  AnnouncementGetQuerySchema,
  ANNOUNCEMENT_LIST_QUERY_FIELDS,
  ANNOUNCEMENT_SORTABLE_FIELDS,
  ANNOUNCEMENT_INCLUDABLE_RELATIONS,
  ANNOUNCEMENT_OMIT_FIELDS,
  API_OMIT_FIELDS_BY_MODEL,
  API_RELATION_TARGETS,
} from '../schemas/validation.js';

const router = new Hono<AppEnv>();

router.get('/', validateQuery(AnnouncementListQuerySchema), async (c) => {
  const db = c.get('db');
  const auth = c.get('auth');
  const policy = assertPolicy('Announcement', auth.role, 'select');
  const policyWhere = resolvePolicyWhere(policy, auth);
  const query = c.req.valid('query');
  const { where, orderBy, take, skip, include } = buildReadQuery(
    query,
    ANNOUNCEMENT_LIST_QUERY_FIELDS,
    ANNOUNCEMENT_SORTABLE_FIELDS,
    ANNOUNCEMENT_INCLUDABLE_RELATIONS,
  );
  const rows = await db.announcement.findMany({
    where: mergeWhere(where, policyWhere),
    orderBy,
    take,
    skip,
    include,
  });
  return c.json(shapeResponseMany(rows, 'Announcement', API_OMIT_FIELDS_BY_MODEL, API_RELATION_TARGETS));
});

router.get('/:id', validateParam(AnnouncementParamSchema), validateQuery(AnnouncementGetQuerySchema), async (c) => {
  const db = c.get('db');
  const auth = c.get('auth');
  const policy = assertPolicy('Announcement', auth.role, 'select');
  const policyWhere = resolvePolicyWhere(policy, auth);
  const params = c.req.valid('param');
  const query = c.req.valid('query');
  const include = query.include
    ? parseIncludeQuery(query.include, ANNOUNCEMENT_INCLUDABLE_RELATIONS)
    : undefined;
  const row = await db.announcement.findUnique(mergeWhere({ id: params.id }, policyWhere), { include });
  if (!row) {
    return notFoundResponse(c);
  }
  return c.json(shapeResponse(row, 'Announcement', API_OMIT_FIELDS_BY_MODEL, API_RELATION_TARGETS));
});

router.post('/', validateJson(AnnouncementCreateSchema), async (c) => {
  const db = c.get('db');
  const auth = c.get('auth');
  const policy = assertPolicy('Announcement', auth.role, 'insert');
  const policyWhere = resolvePolicyWhere(policy, auth);
  const body = c.req.valid('json');
  const row = await db.announcement.create(body, { where: policyWhere });
  return c.json(omitFields(row, ANNOUNCEMENT_OMIT_FIELDS), 201);
});

router.put('/:id', validateParam(AnnouncementParamSchema), validateJson(AnnouncementUpdateSchema), async (c) => {
  const db = c.get('db');
  const auth = c.get('auth');
  const policy = assertPolicy('Announcement', auth.role, 'update');
  const policyWhere = resolvePolicyWhere(policy, auth);
  const params = c.req.valid('param');
  const body = c.req.valid('json');
  const row = await db.announcement.update({ where: mergeWhere({ id: params.id }, policyWhere), data: body });
  return c.json(omitFields(row, ANNOUNCEMENT_OMIT_FIELDS));
});

router.delete('/:id', validateParam(AnnouncementParamSchema), async (c) => {
  const db = c.get('db');
  const auth = c.get('auth');
  const policy = assertPolicy('Announcement', auth.role, 'delete');
  const policyWhere = resolvePolicyWhere(policy, auth);
  const params = c.req.valid('param');
  const row = await db.announcement.delete(mergeWhere({ id: params.id }, policyWhere));
  return c.json(omitFields(row, ANNOUNCEMENT_OMIT_FIELDS));
});

export default router;
