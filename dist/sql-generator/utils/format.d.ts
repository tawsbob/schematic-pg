export declare function joinSection(header: string, statements: string[]): string;
export declare function formatCreateTable(tableName: string, blocks: string[][], options?: {
    partitionBy?: string;
}): string;
